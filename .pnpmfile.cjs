// pnpmfile.cjs - Custom pnpm configuration
// This file allows customizing package installation behavior

// For now, we'll keep this minimal to avoid lockfile conflicts
// The baileys version is managed in package.json

module.exports = {
  hooks: {
    // Add custom hooks here if needed in the future
  },
};
